# DocViewKit Omni 项目规范

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | DocViewKit Omni |
| 文档版本 | 1.8 |
| 产品阶段 | 首发正式版规划 |
| 文档状态 | 0.1.12 已实现 |
| 更新日期 | 2026-08-28 |
| 目标仓库 | `docviewkitOmniForIde` |
| Web 项目 | 独立仓库、独立发布，本仓库仅消费其构建产物 |

本文档是产品范围、宿主接口、质量门槛和发布验收的共同依据。实现细节可以演进，但不得绕过本文规定的产品与安全边界。

## 2. 产品定义

DocViewKit Omni 是面向软件开发者和技术团队的 IDE 内只读文档预览产品。用户在 VS Code 或 JetBrains IDE 中打开仓库、工作区或本地文档时，无需切换到桌面 Office 软件或把文件上传到在线服务，即可查看文档真实内容。

产品由三个独立部分组成：

1. 独立 Web 项目：提供统一预览界面和可嵌入构建产物。
2. VS Code adapter：接入 Custom Editor、Webview 和 VS Code 文件系统。
3. JetBrains adapter：接入 IntelliJ Platform File Editor、JCEF 和 Virtual File System。

DocViewKit 文档解析与渲染能力由 Web 构建产物封装，本仓库不得实现第二套解析器或渲染器。

### 核心价值

- 不离开 IDE 即可查看项目中的办公文档和其他已支持格式。
- 默认本地处理，不把文档上传到第三方服务。
- 两个 IDE 家族保持一致的格式能力、渲染语义和错误诊断。
- 以真实文档结构渲染，不以缩略图或封面图冒充预览结果。

## 3. 目标与非目标

### 3.1 产品目标

1. 在 VS Code 和 IntelliJ IDEA 中提供符合宿主习惯的稳定只读预览。
2. 复用同一 Web 构建产物，使两个插件的显示能力和修复能够同步交付。
3. 将 DocViewKit 延伸到开发工具场景，建立清晰、可搜索的 `DocViewKit Omni` 子产品名称。
4. 在不引入文档上传服务的前提下完成首发正式版交付。

### 3.2 用户目标

- 双击文档即可在编辑区查看，无需寻找外部应用。
- 在查看文档时保留 IDE 的主题、标签页、拆分窗口和项目上下文。
- 文件损坏或部分内容不兼容时，仍能看到可用内容和明确诊断。
- 能确认文档没有被插件静默上传。

### 3.3 首发不包含

- 文档编辑、批注、协同、版本合并和格式转换。
- AI 摘要、问答、翻译或内容生成。
- 云端文档存储、同步、账户系统和后台服务。
- 自研文档解析或渲染逻辑。
- 用内嵌预览图、封面图或缩略图作为最终预览兜底。
- VS Code Web、github.dev、vscode.dev 和 Codespaces 的正式支持承诺。
- 未经逐项兼容验证的 JetBrains 产品宣传。

## 4. 目标用户与核心场景

### 4.1 目标用户

- 在代码仓库中维护需求、设计、测试或交付文档的软件开发者。
- 需要同时审阅代码与 DOCX、XLSX、PPTX、PDF 等资料的技术负责人。
- 使用 JetBrains IDE 或 VS Code 管理文档型测试夹具、模板和客户样例的 QA 与支持人员。

### 4.2 核心场景

1. 用户在文件树中双击受支持文档，文档在普通编辑标签页位置打开。
2. 用户通过“Open With / 打开方式”选择 DocViewKit Omni。
3. 用户修改或替换源文件后，预览安全刷新，并保持可保留的阅读位置。
4. 文档存在局部损坏时，插件显示其余可用内容并提供可复制诊断。
5. 用户切换 IDE 深浅主题，预览外壳随宿主更新，不改变文档本身的颜色语义。

## 5. 首发正式版范围

| 编号 | 能力 | 优先级 | 首发要求 |
| --- | --- | --- | --- |
| F-01 | 文件打开 | P0 | 从 IDE 文件树、编辑器和“打开方式”进入只读预览 |
| F-02 | 真实内容预览 | P0 | 使用固定版本的 Web 构建产物解析和渲染文件 |
| F-03 | Viewer 操作 | P0 | 提供 Web Viewer 已支持的缩放、导航、搜索和视图操作，不在 adapter 重做 |
| F-04 | 多标签与拆分 | P0 | 遵循宿主编辑器生命周期，同一文件可按宿主能力拆分查看 |
| F-05 | 文件变化 | P0 | 检测文件替换或修改，提示或自动安全刷新，不显示陈旧内容 |
| F-06 | 主题与语言 | P0 | 同步宿主深浅主题；首发界面支持中文和英文 |
| F-07 | 错误与诊断 | P0 | 区分不支持、损坏、加密、资源限制、JCEF 不可用和内部错误 |
| F-08 | 本地与离线 | P0 | 插件包内包含运行所需静态资源；预览不依赖远程 Web 服务 |
| F-09 | 安全外链 | P0 | 文档链接经安全校验后交给宿主或系统浏览器打开 |
| F-10 | 隐私说明 | P0 | Marketplace、README 和设置页明确说明本地处理边界 |
| F-11 | 远程工作区 | P1 | VS Code Remote 场景通过 `workspace.fs` 验证后再声明支持 |
| F-12 | JetBrains 扩展认证 | P1 | IDEA 之外的 IDE 逐个通过兼容测试后加入支持列表 |
| F-13 | 可复制支持包 | P1 | 用户主动导出不含文档内容和绝对路径的诊断摘要 |

## 6. 支持格式规则

1. 支持格式的唯一事实来源是 Web 项目发布产物中的机器可读清单。
2. 两个 adapter 的扩展名声明、MIME 映射、Marketplace 文案和测试矩阵必须由该清单生成或在构建时与其校验。
3. 插件不得把清单外格式宣传为“支持”；实验性或部分支持必须明确标注。
4. 格式包缺失时必须报告配置或打包错误，不得退回系统缩略图。
5. 格式的解析、降级和保真边界由 DocViewKit Viewer 决定，adapter 只负责安全传递文件和展示结果。

## 7. 仓库与模块结构

```text
docviewkitOmniForIde/
├── AGENTS.md
├── PROJECT_SPEC.md
├── README.md
├── package.json
├── viewer/       # 共享宿主消息接口，不含渲染器
├── scripts/      # 固定 npm 发布物并生成校验清单
├── test/         # 共享产物、真实浏览器与双包契约测试
├── vscode/
└── jetbrains/
```

### 7.1 模块职责

**VS Code adapter**

- TypeScript 实现。
- 注册只读 Custom Editor 与文件选择器。
- 管理 Webview、CSP、文件读取、消息通信、主题和生命周期。
- 生成并验证 VSIX。

**JetBrains adapter**

- Kotlin 实现。
- 注册 IntelliJ Platform File Editor。
- 管理 JCEF、Virtual File、消息通信、主题和生命周期。
- 在创建预览前检查 `JBCefApp.isSupported()`。
- 生成并验证 JetBrains Marketplace 插件包。

**Web 项目**

- 不进入本仓库。
- 输出可离线嵌入、不可变、带版本号和校验和的构建产物。
- 封装 DocViewKit Viewer、格式包、界面、语言和共享诊断语义。

## 8. Web 构建产物接口

### 8.1 产物要求

每个可用于插件发布的 Web 产物必须包含：

- 不可变版本号。
- SHA-256 校验和。
- HTML、CSS、JavaScript、Wasm、字体和必要格式包。
- 机器可读的支持格式清单。
- 许可证与第三方通知。
- 与 adapter 兼容的接口版本。

插件构建必须固定产物版本和校验和。禁止引用 `latest`、运行时远程地址或未经校验的本地目录。

首版冻结为 `@docviewkit/viewer@0.2.53`，0.1.6 双宿主统一升级为 `@docviewkit/viewer@0.2.54`，0.1.11 双宿主统一升级为 `@docviewkit/viewer@0.2.56`，0.1.12 双宿主统一升级为 `@docviewkit/viewer@0.2.58`，宿主接口版本保持 `1`。npm lockfile 固定 registry integrity，构建产物 `manifest.json` 再记录每个 Viewer 文件的 SHA-256；VSIX 与 JetBrains ZIP 必须通过双包清单和逐文件哈希一致性测试。

### 8.2 最小宿主消息语义

宿主到 Web：

- `open`：传递待打开文件的受控资源引用或字节数据。
- `reload`：源文件改变后重新载入。
- `theme`：同步宿主主题令牌。
- `locale`：设置界面语言。
- `dispose`：释放当前文档和浏览器资源。

Web 到宿主：

- `ready`：界面和运行时就绪。
- `document-ready`：已生成有意义的文档内容。
- `diagnostic`：结构化诊断，不包含文档正文。
- `open-external`：请求宿主安全打开链接。
- `fatal-error`：无法生成有意义内容或继续处理存在安全、资源风险。

接口必须带版本；未知消息和未知字段应安全忽略并记录诊断，接口主版本不兼容时构建失败。

### 8.3 文件传递要求

- adapter 使用宿主文件系统读取文件，不让 Webview/JCEF 任意读取本地路径。
- 不通过 Base64 复制大文件；优先使用宿主支持的受控资源或二进制传输。
- 文件名仅用于显示与格式判断，不得成为不受控 URL、脚本或 HTML。
- 源文件修改时取消旧任务，避免旧结果覆盖新文件。

## 9. 平台要求

### 9.1 VS Code

- 首发支持 VS Code Desktop 在 Windows、macOS 和 Linux 上的最新两个稳定版本。
- 使用 `CustomReadonlyEditorProvider` 或当前官方等价只读接口。
- Webview 必须设置严格 CSP；脚本、样式和资源仅来自插件包批准范围。
- 使用 VS Code URI 与文件系统接口，禁止拼接裸 `file://` 地址。
- 插件停用、标签页关闭、文件切换时释放 Webview 和文件监听资源。

### 9.2 JetBrains

- 首发正式支持 IntelliJ IDEA Community 与 Ultimate 的最新两个稳定版本。
- 使用 IntelliJ Platform 公共接口，不依赖内部或已弃用接口。
- 使用 JCEF 承载 Web 构建产物；JCEF 不可用时提供明确的运行环境说明。
- JCEF 客户端、查询对象、浏览器和监听器必须随 File Editor 正确释放。
- 其他 JetBrains IDE 只有在安装、打开、主题、关闭和资源释放测试通过后才可加入正式支持列表。

## 10. 功能验收

### 10.1 打开文档

- **Given** 用户在受支持的 IDE 中打开格式清单内的健康文档
- **When** DocViewKit Omni 被选为打开方式
- **Then** 文档在编辑区标签页中显示真实解析结果
- **And** 不启动外部桌面应用、不上传文件。

### 10.2 局部损坏

- **Given** 文档包含一个损坏或未知的非关键资源
- **When** Viewer 能隔离该错误并继续解析
- **Then** 其余可用页面、工作表、幻灯片或内容继续显示
- **And** 用户能复制对应诊断。

### 10.3 不支持的运行环境

- **Given** JetBrains 运行时不支持 JCEF
- **When** 用户打开受支持文档
- **Then** 插件显示不可用原因和修复建议
- **And** 不使用缩略图或远程预览兜底。

### 10.4 文件更新

- **Given** 预览已打开
- **When** 源文件内容发生变化
- **Then** 插件取消过时任务并加载最新内容
- **And** 旧任务不得覆盖新结果。

### 10.5 两端一致性

- **Given** 同一 Web 产物和同一文档夹具
- **When** 分别在 VS Code Webview 与 JetBrains JCEF 中打开
- **Then** 两端对象数量、页面单位、诊断类别和关键视觉快照满足统一契约
- **And** 宿主差异仅限主题、窗口尺寸和平台字体等已记录边界。

## 11. 用户体验要求

- 默认入口是普通文件打开，不新增独立工作台或侧栏。
- Viewer 已有命令不得在宿主工具栏重复实现，宿主层只放置打开方式、刷新和复制诊断等必要动作。
- 加载期间显示可取消的进度状态；不得用文档内嵌预览图伪装完成状态。
- 空状态、错误提示和操作名称使用中性、任务导向语言，不使用夸张营销措辞。
- 键盘用户可以进入和离开预览、操作工具栏、搜索、导航并看到焦点。
- 深浅主题只改变应用外壳与控件，不改写文档定义的背景、字体或颜色。

## 12. 性能、可靠性与资源要求

- adapter 引入的首次可见预览额外耗时不得超过对应 Web 基准的 15%。
- 打开第二个标签页不得重复加载可安全共享的不可变运行时资源。
- 关闭最后一个预览后必须释放该文档字节、监听器、浏览器实例和临时资源。
- 超大文件、压缩炸弹、异常页面尺寸、恶意链接和资源耗尽保护沿用 Viewer 安全限制；adapter 不得放宽。
- 单个标签页失败不得导致 IDE、其他标签页或其他预览会话崩溃。
- 构建和测试必须能在干净检出、无 Web 项目源码的环境中完成。

## 13. 安全与隐私

- 文档内容默认仅在本地进程或 IDE 明确连接的工作区环境中处理。
- 首发不采集行为遥测，不发送文档名、绝对路径、正文、哈希或预览截图。
- 不加载远程脚本、远程样式、远程 Wasm 或远程字体。
- 文档中的外部链接在打开前校验协议，只允许明确批准的安全协议并交给宿主处理。
- Webview/JCEF 页面不得获得不必要的 Node、JVM、Shell 或任意文件系统能力。
- Marketplace 包必须包含依赖许可证、第三方通知和构建产物校验记录。
- 用户主动提交诊断时，默认移除文档正文、文件名、绝对路径、账户信息和机器唯一标识。

## 14. 测试与发布门槛

### 14.1 共同测试

- Web 产物版本、接口版本和 SHA-256 校验。
- 支持格式清单与两个 adapter 文件选择器一致性。
- 宿主消息契约的正常、未知消息、取消、超时和销毁路径。
- 真实文档夹具的成功、局部降级、整体失败和资源限制路径。
- 零文档上传、零远程运行时代码请求的网络断言。

### 14.2 VS Code 门槛

- TypeScript、lint、单元测试和扩展宿主集成测试通过。
- 在 Windows、macOS、Linux 的目标 VS Code 版本完成打开、刷新、拆分、关闭测试。
- VSIX 在干净环境安装并离线打开测试文档。
- CSP、资源 URI 和工作区信任行为通过安全检查。

### 14.3 JetBrains 门槛

- Kotlin、Gradle、Plugin Verifier 和集成测试通过。
- 在目标 IntelliJ IDEA Community/Ultimate 版本完成打开、刷新、拆分、关闭测试。
- 在 Windows、macOS、Linux 验证 JCEF、HiDPI、主题与资源释放。
- 插件包在干净环境安装并离线打开测试文档。

### 14.4 发布完成定义

一次平台发布只有在以下条件全部满足后才算完成：

1. 对应提交和版本标签已推送。
2. 平台构建、测试和安全门槛通过。
3. Marketplace 中的目标版本可公开安装。
4. 安装后的包使用规范指定的 Web 产物版本与校验和。
5. 从 Marketplace 安装的真实包完成最小冒烟测试。

两个 Marketplace 使用独立版本号和发布流水线，不要求审批时间同步；产品兼容矩阵必须记录它们各自包含的 Web 产物版本。

## 15. 指标

### 15.1 质量指标

| 指标 | 首发门槛 |
| --- | --- |
| 健康测试文档打开成功率 | 100% 通过受控发布语料 |
| 局部损坏隔离 | 所有指定降级夹具保留其余可用内容 |
| 两端 Web 产物一致性 | 版本与 SHA-256 完全一致 |
| 预览期间文档上传 | 0 |
| 阻断级安全问题 | 0 |
| 插件导致 IDE 崩溃 | 0 个已知可复现问题 |

### 15.2 产品指标

- 两个 Marketplace 的有效安装量与卸载反馈。
- 用户主动提交的问题中，成功附带脱敏诊断的比例。
- Marketplace 评分和“无法打开文档”类反馈占比。
- 从 DocViewKit Omni 到 DocViewKit 官网、文档或 SDK 的用户主动访问，不进行隐蔽追踪。

## 16. 主要风险与措施

| 风险 | 影响 | 措施 |
| --- | --- | --- |
| Web 产物与 adapter 接口漂移 | 插件无法打开或行为不一致 | 固定接口版本与校验和，构建期拒绝不兼容版本 |
| JCEF 在部分运行时不可用 | JetBrains 无法渲染 | 启动前检测并给出明确诊断，不承诺无验证环境 |
| 大文件跨宿主复制导致内存峰值 | IDE 卡顿或崩溃 | 使用受控资源或二进制传输，禁止 Base64 大文件 |
| 两端格式声明漂移 | 同名产品能力不一致 | 由机器可读清单生成或构建时校验 |
| 远程工作区路径语义不同 | 文件无法读取或泄露路径 | 使用宿主文件系统接口，远程支持单独验收 |
| Marketplace 审批不同步 | 两端版本暂时不一致 | 独立版本和兼容矩阵，不阻塞已验证平台发布 |
| 插件范围扩张为编辑器或云服务 | 延期和维护成本上升 | 首发严格保持只读、本地、无后台边界 |

## 17. 里程碑

### M1：接口冻结

- [x] Web 构建产物格式、接口版本和支持格式清单确定。
- [x] 两个 adapter 的最小消息契约确定。
- [x] VS Code 最低版本 `1.125`，IntelliJ Platform 最低 build `252`。

### M2：VS Code 正式版候选

- [x] 完成只读 Custom Editor 与 Webview 集成。
- [ ] 通过真实文档、离线、安全和三系统测试。
- [x] 生成 VSIX；三系统安装验证保留为发布门槛。

### M3：JetBrains 正式版候选

- [x] 完成 File Editor 与 JCEF 集成。
- [ ] 通过真实文档、JCEF、HiDPI、资源释放和三系统测试。
- [x] 生成并通过结构验证；多 IDE Plugin Verifier 与三系统安装验证保留为发布门槛。

### M4：双 Marketplace 发布

- [ ] 两个平台公开版本可安装。
- [ ] Marketplace 安装包冒烟测试通过。
- [ ] 官网、README、隐私说明和支持格式文案一致。

## 18. 待确认事项

以下事项不阻塞项目目录和 adapter 骨架，但必须在对应里程碑前确定：

1. Marketplace 商业模式与 IDE 离线商业许可模型；首版保留 Viewer 原有未授权/Free 品牌行为。
2. 是否在首发后认证 PyCharm、WebStorm、GoLand、CLion、Rider 和 Android Studio。

## 19. 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-08-25 | 1.0 | 建立 DocViewKit Omni 首发正式版产品、架构和验收规范 |
| 2026-08-25 | 1.1 | 冻结 Viewer 0.2.53 与接口 1，记录 0.1.0 双宿主实现和本地验收边界 |
| 2026-08-25 | 1.2 | 双宿主升级 Viewer 0.2.54，隐藏打印与全屏并启用三模式切换 |
| 2026-08-25 | 1.3 | 在桌面宽度居中三模式切换器并保留原生紧凑布局 |
| 2026-08-25 | 1.4 | 统一桌面工具栏 16px 安全边距，避免右侧末按钮贴边 |
| 2026-08-25 | 1.5 | 以真实 VS Code Webview 校准桌面工具栏为 32px 安全边距并增加裁切祖先回归 |
| 2026-08-25 | 1.6 | 清除 VS Code Webview 默认页面内边距，按真实可视边界保证工具栏两侧对称 |
| 2026-08-28 | 1.7 | 双宿主统一升级 Viewer 0.2.56，并增加 Marketplace 自动发布与单市场重试流程 |
| 2026-08-28 | 1.8 | 双宿主统一升级 Viewer 0.2.58，补充统一品牌图标并按官网能力重写 Marketplace 简介 |
